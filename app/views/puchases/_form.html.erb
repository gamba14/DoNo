<%= form_for(puchase) do |f| %>
  <% if puchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(puchase.errors.count, "error") %> prohibited this puchase from being saved:</h2>

      <ul>
      <% puchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :cost %>
    <%= f.text_field :cost %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
